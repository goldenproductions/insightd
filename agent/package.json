{
  "name": "insightd-agent",
  "version": "1.0.0",
  "description": "Insightd agent — collects Docker and host metrics, publishes via MQTT",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "dockerode": "^5.0.0",
    "mqtt": "^5.10.0",
    "node-cron": "^4.5.0"
  },
  "devDependencies": {
    "tsx": "^4.22.4"
  }
}
